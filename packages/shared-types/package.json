{
  "name": "@ecompilot/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for EcomPilot PL",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "type-check": "tsc --project tsconfig.json --noEmit",
    "clean": "rimraf dist .tsbuildinfo",
    "lint": "eslint src/**/*.ts"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}

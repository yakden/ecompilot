{
  "name": "@ecompilot/event-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "NATS JetStream event contracts (Zod schemas) for EcomPilot PL",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "type-check": "tsc --project tsconfig.json --noEmit",
    "clean": "rimraf dist .tsbuildinfo",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@ecompilot/shared-types": "workspace:*",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
